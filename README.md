# IRI Generator
IRI Generator replaces IRIs in the input document with a new ones, based on templates.

Features:
- JSON-LD (optionally JSON5) based configuration
- possibility to filter IRIs to be replaced 
- new IRI is constructed based on the properties of the resource (configurable via SPARQL).

Check the [test cases](src/test/resources/irigenerator) to see how it works.

## Use-case: Back-end renames temporary IRIs generated by front-end.

Having an RDF resource with an IRI:
```
@prefix eczm: <https://example.cogni.zone/model#> .
https://resource/2 a eczm:Two ; 
    eczm:year "2021" ;
    eczm:sequence "0005" .
```

created on the frontend, we want to generate the IRI `https://example.cogni.zone/2021/0005` based on:
- a template `#{[baseUri]}/#{[year]}/#{[sequence]}`
- a `baseUri` of `https://example.cogni.zone`
- a SPARQL query supplying the values for `year` and `sequence`
```
PREFIX eczm: <https://example.cogni.zone/model#> 
SELECT ?year ?sequence {
  <#{[uri]}> eczm:year ?year ;
  eczm:sequence ?sequence
}
```
where `#{[uri]}` points to the original IRI of the resource (`https://resource/2`).

To pick the correct template for the given IRI another SPARQL query is used:
```
PREFIX eczm: <https://example.cogni.zone/model#> .
SELECT ?uri { ?uri a eczm:Two }
```

## Dependencies
The project has been extracted from [Asquare](https://github.com/cognizone/asquare).